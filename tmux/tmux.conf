# set -g prefix C-a
# unbind C-b
# bind C-a send-prefix
set-option -g base-index 1
set -g base-index 1
setw -g pane-base-index 1
set -g mouse on
setw -g mode-keys vi
set -g escape-time 0
set -g repeat-time 1000
set -g history-limit 10000
set -g set-clipboard on
set -g renumber-windows on
set -g detach-on-destroy off
set -g default-terminal "${TERM}"
set -sg terminal-overrides ",*:RGB"

# Style
set -g status-position top
set -g status-justify left
set -g status-style 'bg=#0F1216'
set -g status-left '#[fg=green,bold]   #S #[default] '
set -g status-right ''
set -g window-status-current-format ' '
set -g window-status-format ' '
set -g window-status-current-style '#{?window_zoomed_flag,fg=green,fg=white,bold}'
set -g window-status-bell-style 'fg=red,bold'
## color borders
set -g pane-border-style 'fg=#30363D,bold'
set -g pane-active-border-style 'fg=#30363D,bold'
set -g popup-border-style 'fg=#30363D,bold'

# bindings
bind r source-file ~/.config/tmux/tmux.conf \; display "Reloaded!"
bind C-v split-window -h
bind C-h split-window -v
bind / copy-mode \; send-keys / # search mode
bind-key k display-popup -E -w 62% -h 38% -x C -y C 'nu --login -i -c kpuf'

# List of plugins
set -g @plugin 'tmux-plugins/tpm'

set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'christoomey/vim-tmux-navigator'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
