name: Doctor
windows:
  - name: mr-robot
    root: ~/.Mr Robot
    layout: main-vertical
    panes:
      - msfconsole -h
      - commands:
        - ls
        - echo "sudo nmap -sS -Pn -A [server] > [file]" | cowsay
      - nikto
